# 实现状态矩阵

> 基线日期：2026-08-28。本文是事实状态，不是目标设计；目标与验收标准见 [`../08-复赛改造技术方案.md`](../08-复赛改造技术方案.md)。

## 状态定义

| 状态 | 可使用条件 |
|---|---|
| 已实现 | 代码存在、自动化测试通过，并在对应 Demo 中真实调用 |
| 模拟实现 | 通过有状态 Mock 产生真实输入、输出和状态变化，边界明确 |
| 规划 | 只有设计、静态文件或接口，尚无可运行证据 |
| 外部待验证 | 仓库产物已准备，但必须在 AgentTeams / Cloud Studio 等目标环境运行后才能确认 |

## 里程碑状态

| 里程碑 | 当前状态 | 证据 | 退出条件 |
|---|---|---|---|
| M0 事实口径冻结 | 已实现 | `config/project-facts.json`、版本化 Policy、Incident Schema、README | 机器可读清单与文档统计一致 |
| M1 有状态核心 | 规划 | 仅有目标 Schema | SQLite 写入可重查，相同 seed 可重复 |
| M2 冷柜五阶段闭环 | 规划 | 改造前静态 CSV Demo 不计入 | A、D、E 本地端到端通过 |
| M3 AgentTeams 真运行 | 外部待验证 | v1.2.3 官方格式已锁定，Worker/YAML 尚未改造 | Team Room、委派、MCP 和 Trace 同一任务成功 |
| M4 测试、评测和证据 | 规划 | 尚无 P0 测试目录 | 6 个确定性场景 100% 通过 |
| M5 文档、PPT 和视频 | 规划 | 现有材料为改造前版本 | 所有声明可追溯到代码、测试或外部证据 |

## 冻结决策

- 最终负责人：XZQ；M0～M5 均由 XZQ 负责，暂无备份人。
- AgentTeams：`v1.2.3`，CRD `agentteams.io/v1beta1`，Manager / Worker 均选 `qwenpaw`。
- 官方 Tag 文档与 Release 已核对；本机没有 Docker、Kubernetes 和 `agt`，运行证据必须在可用目标环境补齐。
- Skill：9 个目标 Skill，P0 为 6 个；MCP：12 个 P0 函数。
- 冷链策略来源：仓库内 `coldchain-demo:1.0.0`，只用于合成比赛场景，不宣称监管合规。
- 领域 Schema：`IncidentCase v1`；后续不兼容修改必须升级 Schema 版本。

## 当前风险

1. 改造前 Demo 仍按一个 `TaskContext` 混合多个异常，且静态 CSV 时间锚点已过期，不能证明事件级安全关闭。
2. 既有审批默认自动通过，Auditor 只检查动作记录，不能作为独立验证证据。
3. 既有 AgentTeams YAML 的包 URI 和命令不符合锁定版本；M3 前不得宣称可部署。
4. 既有 PDF、01～07 文档仍包含旧统计或过度声明；M5 必须按最终实现统一更新。

## 更新规则

- 每个里程碑完成后更新本矩阵，并在同一提交中附上实际验证命令。
- 不能在本机运行的外部能力维持“外部待验证”，不得用静态 YAML 或截图模板替代真实证据。
- 任何数字、能力或版本发生变化，先更新 `config/project-facts.json`，再同步人类可读文档。
