# 逐光队 Worker 实际部署服务器核查

核查及迁移日期：2026 年 9 月 17 日，23:01–23:18（北京时间）。

## 结论

逐光队采用跨服务器部署。广州运行控制端、Manager、店巡 MCP 和展示主页；当前五个 Worker 分布在上海、首尔、新加坡和 AWS 东京。

本次现场确认上海的 Orchestrator、Sentry，首尔的 Diagnoser，以及新加坡的 Auditor 正在运行。用户确认雅加达腾讯已下线，并授权迁往 PHX 或东京后，已将 Executor 部署到 AWS 东京。至此 **5 个 Worker 容器均有本次现场运行证据**。

这里的“正在运行”指 Docker 容器状态；本次没有触发业务任务，不能据此认定模型调用、跨 Agent 协作或事件闭环已经成功。

## Worker 与服务器对应表

| Worker | 职责 | 服务器 | 公网 IP | Tailscale IP | 容器名称 | 本次结果 |
|---|---|---|---|---|---|---|
| Orchestrator | 总控、计划和任务委派 | 上海 `sh` | `106.54.232.63` | `100.110.32.124` | `agentteams-worker-orchestrator` | 已验证：running，Up 2 weeks |
| Sentry | 巡检、证据采集和异常分级 | 上海 `sh` | `106.54.232.63` | `100.110.32.124` | `agentteams-worker-sentry` | 已验证：running，Up 2 weeks |
| Diagnoser | 风险评估和根因诊断 | 首尔 `kr` | `150.109.247.202` | `100.99.192.47` | `agentteams-worker-diagnoser` | 已验证：running，Up 2 weeks |
| Executor | 审批后的受控执行 | AWS 东京 `aws-tokyo` | `18.183.74.183` | `100.113.73.94` | `agentteams-worker-executor` | 已迁移：running，heartbeat ready，Agent idle |
| Auditor | 独立核验、复盘和关闭建议 | 新加坡 `sg` | `43.134.97.88` | `100.83.43.77` | `agentteams-worker-auditor` | 已验证：running，Up 2 weeks |

现场确认的 5 个 Worker 均使用镜像：

```text
higress-registry.cn-hangzhou.cr.aliyuncs.com/agentteams/agentteams-qwenpaw-worker:v1.2.3
```

原有四个容器采用 Docker `bridge` 网络，未显示宿主机端口发布；东京 Executor 同样使用默认 bridge 网络，没有发布宿主机端口。本报告不将主页旧快照里的 Worker 端口当成当前外部访问地址。

### 容器启动时间

下表为 Docker `State.StartedAt`，统一转为北京时间。这是容器启动时间，不是最近一次业务成功时间。

| Worker | 启动时间（北京时间） |
|---|---|
| Orchestrator | 2026-09-03 16:28:52 |
| Sentry | 2026-09-03 16:52:58 |
| Diagnoser | 2026-09-03 16:56:01 |
| Auditor | 2026-09-03 16:59:08 |
| Executor（东京） | 2026-09-17 23:14:05 |

## 广州控制端与代码位置

广州服务器公网 IP 为 `119.29.105.15`，Tailscale IP 为 `100.116.162.22`，现场主机名为 `VM-0-4-ubuntu`。

| 组件 | 实际部署位置或入口 | 本次证据 |
|---|---|---|
| Controller | Docker 容器 `agentteams-controller` | Up 2 weeks，镜像 `agentteams-embedded:v1.2.3` |
| Manager | Docker 容器 `agentteams-manager` | Up 2 weeks，镜像 `agentteams-manager-qwenpaw:v1.2.3` |
| 店巡 MCP | 工作目录 `/home/ubuntu/agentteams-competition/zhuguang` | 进程 `dianxun-mcp`，PID `1257097`，监听 `100.116.162.22:18090` |
| 项目代码 | `/home/ubuntu/agentteams-competition/zhuguang` | Git HEAD 为 `151e92ec0d6a96fb73335969bfac9832fbef9b40` |
| Manager 工作区 | `/home/ubuntu/agentteams-competition/manager-workspace` | Controller、Manager 的 Docker 挂载记录已确认 |
| 部署清单 | `/home/ubuntu/agentteams-competition/runtime/manifests` | 存在五个 Worker、Manager 与 Team 的 YAML |
| 逐光主页 | `/var/www/mazhi.icu/zhuguang/` | Nginx 的 `/zhuguang/` 路由使用 `/var/www/mazhi.icu` 作为 root |

代码目录 HEAD 提交标题为 `ci: isolate test and artifact verification budgets`，提交时间为 `2026-09-15T23:29:18+08:00`。该 HEAD 只证明广州目录检出的版本，不代表正在运行的旧进程、Worker 镜像和主页都来自同一提交；本次没有验证整个工作树是否干净，也没有确认各 Worker 内的业务包与该提交完全一致。

Controller 的宿主机端口映射为 `127.0.0.1:28001→8001`、`127.0.0.1:28080→8080`、`127.0.0.1:28088→8088`；Manager 为 `127.0.0.1:28888→18799`。这些是本机入口，不应直接视为公网服务。

## 实际核查路径

1. 本地 Mac 的 Tailscale 显示退出登录，控制服务器 DNS 解析失败。
2. 通过公网 SSH 登录广州，读取容器、进程、监听端口、Nginx 路由及指定项目目录。
3. 广州的 Tailscale 正常，读取到上海、首尔、新加坡、雅加达腾讯等节点地址。
4. 通过广州跳转上海 Tailscale 地址，确认 Orchestrator、Sentry 容器。
5. 通过广州既有 `seoul` SSH 别名连接首尔公网地址，确认 Diagnoser 容器。
6. 广州到新加坡的 Tailscale SSH 公钥认证失败；随后通过本地到新加坡公网 SSH 成功，确认 Auditor 容器。
7. 广州到雅加达腾讯的 Tailscale、公网 SSH 均连接超时。广州 `tailscale status` 显示该节点 offline、last seen 4d ago。此证据不能单独证明主机关机或容器停止。

## 历史证据与当前状态的区别

广州文件 `/home/ubuntu/agentteams-competition/status/public-run.json` 的采集时间为 `2026-09-03T09:00:00Z`，记录了旧服务器映射：Orchestrator/Sentry→sh、Diagnoser→kr、Executor→yjd-tx、Auditor→sg。Executor 的旧位置已被本次东京部署取代；该历史文件未改写，不应当成当前拓扑。

该历史文件记录业务运行曾受 Provider 额度问题阻塞，不能将其作为当前故障结论。

另一个本地记录 `/home/ubuntu/agentteams-competition/runtime/worker-auth-verification-20260916.json` 显示，9 月 16 日曾核验 Orchestrator、Sentry、Diagnoser 的鉴权修复；它同时注明 Executor、Auditor 当时未核验、完整业务链未验证。该文件同样属于历史证据，不能替代当前健康检查。

主页地址：[逐光队主页](https://mazhi.icu/zhuguang/)。此前本会话读取的[公开状态文件](https://mazhi.icu/zhuguang/status.json)生成时间仍是 9 月 3 日，并存在 Worker 状态字段冲突。因此本报告以 SSH 现场容器记录为当前部署依据。

## Executor 东京迁移结果

- 服务器：AWS 东京，公网 `18.183.74.183`，Tailscale `100.113.73.94`，系统账户 `ec2-user`，Amazon Linux 2023 / x86_64。
- 资源：迁移前可用内存约 7.3 GB、磁盘剩余约 6.2 GB。安装 Amazon Linux 官方仓库 Docker；未新建或扩容云实例。
- 复用广州保存的 Executor 身份和 MinIO 工作区；容器镜像版本为 v1.2.3，拉取摘要 `sha256:6bc61f3857c5cd290d5f75f2d07eb584b6560cdacdce373a7aedf786f95abd0d`。
- 容器：`agentteams-worker-executor`，配置 `unless-stopped`；内存上限 2 GB、CPU 上限 1.5 核；日志轮转 10 MB × 3；没有公网端口映射。
- 持久化卷：`zhuguang-executor-workspace` → `/root/agentteams-fs`。项目配置目录为 `/home/ubuntu/agentteams-competition`（为兼容既有续期程序保留这个目录名，SSH 登录账户仍是 ec2-user）。
- 已加入广州现有 `zhuguang-worker-token-renew.timer`，每 20 分钟续期，令牌有效期 1 小时。Executor 单独续期实测成功；未等待整个一小时过期窗口。
- 广州至东京的项目 SSH 公钥限制来源为 `100.116.162.22`，禁用转发等扩展能力；私钥留在广州，AWS 登录私钥未复制到服务器。

### 验收证据

| 检查 | 结果 |
|---|---|
| Docker 状态 | running，RestartCount=0 |
| 本地 Agent 接口 | HTTP 200，status=idle |
| Worker 到广州控制端鉴权 | HTTP 200，令牌续期后再次验证通过 |
| Worker 心跳 | status=ready，updatedAt=`2026-09-17T15:17:14.801455+00:00` |
| 控制端确认 | 日志出现 `controller ready report accepted` |
| Matrix | 已登录 Executor 身份，并启动同步循环 |
| 店巡 MCP | 日志确认 `MCP client connected: dianxun-mcp` |
| 工作区恢复 | IDENTITY.md、AGENTS.md、TEAMS.md 均存在 |
| Executor Skill | 工作区及 agent-package 中均存在 `work-order-dispatch/SKILL.md` |

**保留问题：** 控制端 Worker 查询仍返回 `phase=Pending`、`containerState=not_found`，与东京容器及 ready 心跳不一致。此前控制端记录过 Skill 分发警告。本次未改写控制端状态或强行标记成功；控制端展示一致性仍待修复。业务任务、模型成功调用及完整审批执行链尚未验证。

## 尚未完成的验证

- 原有四个 Worker：尚未进行最新鉴权、模型调用、MCP 权限和完整任务闭环验证。
- Executor：基础连接验收通过，控制端状态一致性及完整业务闭环仍待验证。
- 版本一致性：尚未验证广州代码、五个 Worker 业务包、主页与 GitHub `marongwork/zhuguang` 当前分支的一致性。

初始盘点为只读；随后按用户授权安装东京 Docker、部署 Executor、接入续期并建立本机后台 SSH 隧道。没有主动下发业务执行任务，没有修改其他四个 Worker。文档未包含密钥、令牌或密码。

后台入口与连接方法见同目录《AgentTeams后台入口与使用说明-2026-09-17.md》。
