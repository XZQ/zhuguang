# 店巡 Agent Python 依赖
# 设计原则:demo 仅用 Python 标准库(零依赖,评审可离线跑);生产可选依赖标注注释

# === 核心运行时(零依赖,Python 3.9+ 标准库即可) ===
# - csv / sqlite3 / http.server / json / statistics / uuid
# 无需 pip install

# === 生产环境可选(注释,按需启用) ===
# 向量检索(知识库 RAG 升级,替换关键词匹配)
# pgvector==0.3.6            # PolarDB for PostgreSQL 向量扩展

# OpenTelemetry(可观测,替换 SQLite Trace)
# opentelemetry-api==1.27.0
# opentelemetry-sdk==1.27.0
# opentelemetry-genai==0.4.0  # GenAI 语义约定

# MCP SDK(若用官方 MCP Server 实现替代本项目 server.py)
# mcp==1.2.0

# RocketMQ(事件驱动,替换内存队列)
# rocketmq-client-python==2.0.0

# ASGI Server(MCP Server 生产部署,替代 http.server)
# uvicorn==0.30.0
# fastapi==0.111.0

# LLM SDK(Agent 决策从规则引擎升级为 LLM)
# dashscope==1.20.0          # 阿里云百炼/Qwen
# openai==1.40.0             # OpenAI 兼容(DeepSeek 等)
