# 安全策略

## 支持范围

逐光当前版本为 `0.2.0.dev0` 开发预览。安全修复只面向 `main` 最新代码；尚未承诺长期支持版本、生产 SLA 或食品安全合规认证。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 提交私密报告。若该入口不可用，请先通过仓库所有者的 GitHub 主页联系维护者，只描述影响范围并请求私密沟通渠道，不要在公开 issue 中发布利用细节、凭据、真实身份或业务数据。

报告建议包含：受影响提交、复现前置条件、最小复现、预期/实际结果、影响评估和建议修复。不要针对非授权系统、真实门店或第三方账号进行探测。

维护者会先确认收到并进行分级；修复、披露和版本安排取决于影响与可复现性。当前项目没有承诺固定响应时限。

## 安全边界

- Bearer Token 只从 Secret 或环境变量注入，不能进入源码、日志、截图、Trace 或测试 fixture。
- MCP 非回环监听必须鉴权；共享 Token 仅允许只读，写动作必须绑定 Actor 并通过工具级角色白名单。
- 冷链策略、合成场景和本地评测不能作为真实商品放行、监管合规或生产可用性依据。
- 托管 PolarDB、OSS、AgentTeams 动态身份和真实 HITL 在取得目标环境证据前均属于外部待验证。
